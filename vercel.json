{
  "version": 2,
  "buildCommand": "cd mr-gift-app && npm install && npm run build",
  "outputDirectory": "mr-gift-app/dist",
  "installCommand": "cd mr-gift-app && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}