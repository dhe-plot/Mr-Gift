{
  "version": 2,
  "buildCommand": "cd mr-gift && npm install && npm run build",
  "outputDirectory": "mr-gift/.next",
  "installCommand": "cd mr-gift && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}